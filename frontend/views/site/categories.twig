<div class="owl-carousel categories__carousel">
    {% for item in ITEMS %}
        <div class="category">
            <img src="{{ item.image }}" alt="category" class="category__image" data-id="{{ item.id }}" />
            <p class="category__title">{{ t('database', item.name) }}</p>
        </div>
    {% endfor %}
</div>