# Static Nginx configuration for Docker (without envsubst)
# Frontend - главный сайт
server {
    listen 80 default_server;
    server_name _;
    root /var/www/html/frontend/web;
    index index.php index.html;
    
    charset utf-8;
    client_max_body_size 128M;
    
    access_log /var/log/nginx/frontend-access.log;
    error_log /var/log/nginx/frontend-error.log;
    
    location / {
        try_files $uri $uri/ /index.php?$args;
    }
    
    location ~ \.php$ {
        fastcgi_pass 127.0.0.1:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include fastcgi_params;
        fastcgi_read_timeout 300;
    }
    
    location ~ /\.(ht|svn|git) {
        deny all;
    }
    
    location ~* \.(jpg|jpeg|gif|png|svg|js|css|mp3|ogg|mpeg|avi|zip|gz|bz2|rar|swf|webp|woff|woff2|ico)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
        access_log off;
    }
}

# Backend - админ-панель
server {
    listen 80;
    server_name backend.* e.*;
    root /var/www/html/backend/web;
    index index.php index.html;
    
    charset utf-8;
    client_max_body_size 128M;
    
    access_log /var/log/nginx/backend-access.log;
    error_log /var/log/nginx/backend-error.log;
    
    location / {
        try_files $uri $uri/ /index.php?$args;
    }
    
    location ~ \.php$ {
        fastcgi_pass 127.0.0.1:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include fastcgi_params;
        fastcgi_read_timeout 300;
    }
    
    location ~ /\.(ht|svn|git) {
        deny all;
    }
}

# API
server {
    listen 80;
    server_name api.*;
    root /var/www/html/api/web;
    index index.php index.html;
    
    charset utf-8;
    client_max_body_size 128M;
    
    access_log /var/log/nginx/api-access.log;
    error_log /var/log/nginx/api-error.log;
    
    location / {
        try_files $uri $uri/ /index.php?$args;
    }
    
    location ~ \.php$ {
        fastcgi_pass 127.0.0.1:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include fastcgi_params;
        fastcgi_read_timeout 300;
    }
    
    location ~ /\.(ht|svn|git) {
        deny all;
    }
}

